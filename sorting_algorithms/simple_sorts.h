#ifndef _SIMPLE_SORTS
#define _SIMPLE_SORTS
#include "headers.h"

void insertion_sort(int arr[], int size);
void selection_sort(int arr[], int size); 
void bubble_sort(int arr[], int n); 
void bubble_sort_optimized(int arr[], int n);

#endif
